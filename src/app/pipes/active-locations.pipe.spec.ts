import { ActiveLocationsPipe } from './active-locations.pipe';

describe('ActiveLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
