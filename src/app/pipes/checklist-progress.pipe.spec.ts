import { ChecklistProgressPipe } from './checklist-progress.pipe';

describe('ChecklistProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new ChecklistProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
