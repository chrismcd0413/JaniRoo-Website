import { FormatDateTZPipe } from './format-date-tz.pipe';

describe('FormatDateTZPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateTZPipe();
    expect(pipe).toBeTruthy();
  });
});
